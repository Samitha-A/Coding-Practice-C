

#include<stdio.h>
#include<string.h>
int main()
{
    char arr[1000];
    scanf("%[^\n]",arr);
    int n=strlen(arr);
    int count=1;
    for(int i=0;i<n;i++)
    {
        if(arr[i]==' '|| arr[i]=='\0')
        {
            count++;
        }
    }
    printf("%d",count);
}
