#include <stdio.h>

int main() {
    int arr[] = {10,18,3,7,11,9,4};
    int len = sizeof(arr)/sizeof(arr[0]);
    int max = arr[0];
    for(int i=0; i<=len; i++){
        max=arr[i];
    }
    printf("%d",max);
}
