#include <stdio.h>
#include <string.h>

int main() {
    char str1[100], str2[100];
    int a1 = 0, b1 = 0, c1 = 0;
    int a2 = 0, b2 = 0, c2 = 0;

    scanf("%s %s", str1, str2);
    
    sscanf(str1, "%d.%d.%d", &a1, &b1, &c1);
    sscanf(str2, "%d.%d.%d", &a2, &b2, &c2);

    if (a1 > a2) {
        printf("ver 1 is greater");
    } else if (a2 > a1) {
        printf("ver 2 is greater");
    } else {
        if (b1 > b2) {
            printf("ver 1 is greater");
        } else if (b2 > b1) {
            printf("ver 2 is greater");
        } else {
            if (c1 > c2) {
                printf("ver 1 is greater");
            } else if (c2 > c1) {
                printf("ver 2 is greater");
            } else {
                printf("ver 1 and ver 2 are equal");
            }
        }
    }
}
